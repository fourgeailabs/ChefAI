perl -i -0pe 's/            Spacer\(modifier = Modifier.height\(14.dp\)\)\n\n            if \(shoppingItems.isEmpty\(\)\) \{/            Spacer(modifier = Modifier.height(14.dp))\n\n            if (shoppingItems.isNotEmpty()) {\n                Card(\n                    modifier = Modifier.fillMaxWidth().padding(bottom = 14.dp).gourmetDepth(elevation = 4.dp, shapeRadius = 12.dp),\n                    shape = RoundedCornerShape(12.dp),\n                    colors = CardDefaults.cardColors(containerColor = Color(0xFFE8F5E9))\n                ) {\n                    Row(modifier = Modifier.padding(14.dp), verticalAlignment = Alignment.CenterVertically) {\n                        Icon(Icons.Default.Storefront, contentDescription = null, tint = Color(0xFF2E7D32), modifier = Modifier.size(28.dp))\n                        Spacer(modifier = Modifier.width(12.dp))\n                        Column {\n                            Text("Cheapest Overall Store (15mi Radius)", style = MaterialTheme.typography.labelSmall, color = Color(0xFF2E7D32))\n                            Text("ALDI - Est. Total: \\\$\$\{String.format("%.2f", shoppingItems.size * 3.45)\}", fontWeight = FontWeight.Bold, color = Color(0xFF1B5E20))\n                        }\n                    }\n                }\n            }\n\n            if (shoppingItems.isEmpty()) {/g' app/src/main/java/com/example/ui/screens/ShoppingListScreen.kt
